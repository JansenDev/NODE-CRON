{
  "name": "node-cron",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "npm run build && node dist",
    "dev":"nodemon",
    "dev:fail": "nodemon --exec \"node -r tsconfig-paths/register -r ts-node/register ./src/index.ts\"",
    "build": "tsc && tsc-alias",
    "clean": "rm -rvd dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node-cron": "^3.0.4",
    "node-cron": "^3.0.2",
    "tsconfig-paths": "^4.1.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "tsc-alias": "^1.7.0",
    "typescript": "^4.8.3"
  }
}